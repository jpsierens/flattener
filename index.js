#!/usr/bin/env node

const path = process.argv.slice(2);

const flattener = (rootDir, cb) => {
    console.log(`you chose to flatten ${path}`);
    console.log('But I really havent implemented anything yet :p');
};

flattener(path);